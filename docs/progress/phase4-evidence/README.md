# Phase 4 evidence

This directory is populated automatically by the `phase4-runtime-host` CI
workflow (H1–H8 host gates + G1–G12/G14/G15 re-run against the production
app). It is intentionally empty until that workflow has run — see
`docs/progress/phase4-runtime-host-report.md` §4 for the one-time workflow
install step (the automation token cannot push `.github/workflows/`).
