harness preflight: no working Python interpreter (tried python3, python, py -3).
  On Windows, 'python3' is often a Microsoft Store stub that prints
  "Python was not found" and exits non-zero; install Python 3, or use the
  'py -3' launcher, then re-run. Without it the readers this script drives
  (p10d-ui.py, p10d-png.py, check-apk.py) cannot run at all.
